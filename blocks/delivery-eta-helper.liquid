<div class="eta-widget" data-base-time="{{ block.settings.base_time }}" data-order-time="{{ block.settings.order_time }}" data-cutoff-time="{{ block.settings.cutoff_time }}">
  <div class="delivery-wrapper">
    <label class="delivery-label">Region</label>
    <select class="region">
        <option value="">Select region</option>
        <option value="PH">Philippines</option>
        <option value="US">United States</option>
        <option value="EU">Europe</option>
    </select>
  </div>
  <div class="delivery-wrapper">
    <p class="delivery-date"></p>
    <p class="delivery-message"></p>
  </div>
   <script type="application/json" class="product-json">
        {
            "available": {{ product.available | json }},
            "tags": {{ product.tags | json }},
            "variants": {{ product.variants | json }}
        }
  </script>
</div>

{{ 'delivery.css' | asset_url | stylesheet_tag }}
{{ 'delivery.js' | asset_url | script_tag }}

{% schema %}
{
  "name": "Delivery ETA Helper",
  "settings": [
    {
      "type": "number",
      "id": "base_time",
      "label": "Days (Base Time)",
      "default": 3
    },
    {
      "type": "number",
      "id": "order_time",
      "label": "Days (Order Lead Time)",
      "default": 14
    },
    {
      "type": "number",
      "id": "cutoff_time",
      "label": "Daily Cutoff",
      "default": 15
    }
  ]
}
{% endschema %}